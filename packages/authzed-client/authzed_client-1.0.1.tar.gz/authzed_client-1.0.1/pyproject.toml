[tool.poetry]
name = "authzed-client"
version = "1.0.1"
description = "A client library for accessing Authzed"

authors = []

readme = "README.md"
packages = [
    {include = "authzed_client"},
]
include = ["CHANGELOG.md", "authzed_client/py.typed"]

[tool.poetry.dependencies]
python = "^3.8"
httpx = ">=0.20.0,<0.26.0"
attrs = ">=21.3.0"
python-dateutil = "^2.8.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.ruff]
select = ["F", "I"]
line-length = 120
