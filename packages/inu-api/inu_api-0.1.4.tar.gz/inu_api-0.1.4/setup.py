from setuptools import setup, find_packages

setup(
    name='inu_api',
    version='0.1.4',  # Update the version number
    packages=find_packages(),
    install_requires=[
        # Your package dependencies go here
    ],
    # Other metadata like author, description, etc.
)
