[build-system]
requires = ['hatchling', "hatch-vcs>=0.3.0"]
build-backend = 'hatchling.build'

[project]
name = "sinaraml_cli_host"
authors = [
    {name = "sinaraml", email = "sinaraml.official@gmail.com"},
]
description = "SinaraML Host CLI Plugin"
readme = "README.md"
requires-python = ">=3.6"
keywords = ["cli", "sinaraml"]
license = { file="LICENSE" }
classifiers = [
        "License :: OSI Approved :: Apache Software License",
        "Operating System :: OS Independent",
        "Programming Language :: Python :: 3.6",
        "Programming Language :: Python :: 3.7",
        "Programming Language :: Python :: 3.8",
        "Programming Language :: Python :: Implementation :: CPython",
        "Topic :: Scientific/Engineering :: Artificial Intelligence"
]
dependencies = [
    "sinaraml_cli>=2023.11.17",
    "docker>=4.3.1",
    'importlib-metadata; python_version>="3.6"'
]
dynamic = ["version"]

[project.urls]
"Homepage" = "https://github.com/4-DS/sinaraml_cli_host"

[tool.hatch.version]
source = "vcs"
fallback-version = "0.0.0"
[tool.hatch.build.hooks.vcs]
version-file = "sinaraml_cli_host/_version.py"
[tool.hatch.version.raw-options]
git_describe_command = [
    "git",
    "describe",
    "--dirty",
    "--tags",
    "--long",
    "--first-parent",
]
version_scheme = "post-release"
fallback_version = "0.0.0"
[tool.hatch.metadata]
allow-direct-references = true
[tool.hatch.build.targets.sdist]
only-include = ["sinaraml_cli_host"]
[tool.hatch.build.targets.wheel]
packages = ["sinaraml_cli_host"]