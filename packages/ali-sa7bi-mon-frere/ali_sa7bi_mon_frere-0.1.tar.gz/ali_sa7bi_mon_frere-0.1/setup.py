# setup.py
from setuptools import setup, find_packages

setup(
    name='ali_sa7bi_mon_frere',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
)
