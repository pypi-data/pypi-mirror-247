# pyproject.toml

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "BoxTurtle"
version = "1.1.6"
description = "Simple turtle graphics library with 3D integration"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "chadwickjr", email = "chadmillimanjr@gmail.com" }]
license = { file = "LICENSE" }
keywords = ["turtle", "3D", "graphics"]