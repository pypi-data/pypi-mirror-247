# In case of a periodic domain, input data is "complete" in the x direction.
# With a non-periodic domain, the computational domain is smaller than the input
# data: the outermost rows and columns are used as "ghost cells" in which we can
# insert boundary conditions
