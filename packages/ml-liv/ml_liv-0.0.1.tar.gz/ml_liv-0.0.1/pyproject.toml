[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ml_liv"
version = "0.0.1"
dependencies = [
    "kfp==1.8.9",
    "google-cloud",
]
requires-python = ">=3.8"
authors = [
  {name = "Gabriel Cavalcante Peres", email = "gabriel.peres@leega.com.br"}
]

