[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "housing_library_5507"
version = "0.2"
description = "Sample code for coding practice"
readme = "README.md"
authors = [{ 'name' = "5507lohithareddy", 'email' = "lohitha.reddy@tigeranalytics.com" }]

 
classifiers = [
    "Programming Language :: Python :: 3",
]
keywords = ["housing","data training"]
 
dependencies = [ "numpy", "pandas", "scikit-learn"]
 
requires-python = ">=3.11"

[project.urls]
Homepage = "https://github.com/5507lohithareddy/mle-training"
 
[project.scripts]
ingest_data = "ingest_data:main"
train = "train:main"
score = "score:main"
