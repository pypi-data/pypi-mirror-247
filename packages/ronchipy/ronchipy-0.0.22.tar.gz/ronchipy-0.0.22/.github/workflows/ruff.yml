name: Ruff
on:
    pull_request:
      paths:
        - '**/*.py'

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: chartboost/ruff-action@v1
        with:
          src: "./src/ronchipy"
